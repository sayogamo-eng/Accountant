Put real team photos here (JPG/PNG, at least 800×800, square crop).
Set the file name in `photo` in src/data/team.ts, e.g. `photo: 'dana-cohen.jpg'`.
The build converts them to AVIF/WebP at the right sizes automatically.
