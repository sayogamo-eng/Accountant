# Production domain (canonical, sitemap, Open Graph)
SITE_URL=https://www.example-cpa.co.il
# Form endpoint that receives the contact form as JSON (Web3Forms / Formspree / Make / own API)
PUBLIC_FORM_ENDPOINT=
# Google Analytics 4 measurement ID. Loaded only after the visitor accepts cookies.
PUBLIC_GA_ID=
